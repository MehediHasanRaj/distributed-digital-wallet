spring:

  application:
    name: wallet-service

  datasource:
    url: jdbc:mysql://localhost:3306/wallet_db
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true